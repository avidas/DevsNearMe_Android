<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avidas.localize"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="17" />

    <permission
        android:name="com.avidas.localize.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.avidas.localize.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
     The following two permissions are not required to use
     Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAXJiDCXebCmSchijsJi5FHLnGRfzNkUmg" />
        <meta-data 
            android:name="com.facebook.sdk.ApplicationId" 
            android:value="@string/app_id"/>
        <activity android:name="com.facebook.LoginActivity"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar"
                  android:label="@string/app_name" />
        <activity
            android:name="com.avidas.localize.SignIn"
            android:label="Sign In" >
            <!-- android:label="@string/app_name" FIXME change to appname -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.avidas.localize.MapActivity"
            android:label="@string/title_activity_map"
            android:parentActivityName="com.avidas.localize.SignIn" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.avidas.localize.SignIn" />
        </activity>
        <activity
            android:name="com.avidas.localize.DevAroundActivity"
            android:label="@string/title_activity_map"
            android:parentActivityName="com.avidas.localize.SignIn" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.avidas.localize.SignIn" />
        </activity>        
    </application>

</manifest>
